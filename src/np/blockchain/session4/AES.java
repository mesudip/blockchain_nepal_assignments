package np.blockchain.session4;
import np.blockchain.session1.Cipher;
public class SHA extends np.blockchain.session1.Cipher {

    @Override
    public byte[] encrypt(byte[] data) {
        return new byte[0];
    }

    @Override
    public byte[] decrypt(byte[] data) {
        return new byte[0];
    }
}
